<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="homeAsUpIndicator">@drawable/navicon</item>
        <item name="android:homeAsUpIndicator">@drawable/navicon</item>
        <item name="android:colorPrimary">@color/HotPink</item>
        <!--   darker variant for the status bar and contextual app bars -->

        <item name="android:colorPrimaryDark">@color/HotPink
        </item>
        <item name="android:navigationBarColor">@color/HotPink</item>
    </style>

</resources>
